# pip 配置文件示例
# 使用方法：
# 1. Linux/WSL: 复制到 ~/.pip/pip.conf
# 2. Windows: 复制到 %APPDATA%\pip\pip.ini
# 3. 或者在项目根目录创建 pip.conf，使用 pip install -r requirements.txt --config-file pip.conf
# 4. 单次安装 pip install -r requirements.txt -i https://pypi.tuna.tsinghua.edu.cn/simple

[global]
index-url = https://pypi.tuna.tsinghua.edu.cn/simple
trusted-host = pypi.tuna.tsinghua.edu.cn

# 其他可选镜像源（如果清华源不可用，可以切换）：
# 阿里云: https://mirrors.aliyun.com/pypi/simple/
# 中科大: https://pypi.mirrors.ustc.edu.cn/simple/
# 豆瓣: https://pypi.douban.com/simple/

